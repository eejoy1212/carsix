import 'package:carsix/const/color.dart';
import 'package:flutter/material.dart';

class AutoBrightnessIcon extends StatelessWidget {
  final bool isAutoBrightnessOn;

  AutoBrightnessIcon({required this.isAutoBrightnessOn});

  @override
  Widget build(BuildContext context) {
    return Stack(
      alignment: Alignment.center,
      children: [],
    );
  }
}
